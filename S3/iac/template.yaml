AWSTemplateFormatVerion: "2010-09-09"
Description: A simple S3 Bucket

Resources:
  S3Bucket:
  # https://docs.aws.amazon.com/AWSCloudFormation/latest/TemplateReference/aws-resource-s3-bucket.html
    Type: 'AWS::S3::Bucket'
  # We dont need to supply a bucket name as AWS will generate for us